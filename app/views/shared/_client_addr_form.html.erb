<%= render 'shared/error_messages', :object => f.object %>
<div class="field">
	<%= f.text_area :street %>
</div>
<div class="field">
	<%= f.text_area :city %>
</div>
<div class="field">
	<%= f.text_area :state %>
</div>
<div class="field">
	<%= f.text_area :zip %>
</div>
<div class="actions">
<%= f.submit "Submit" %>
</div>

