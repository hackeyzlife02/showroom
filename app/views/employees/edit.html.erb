<h1>Edit Profile</h1>
<%= render 'shared/error_messages', :person => @employee %>
<div id="edit_client_form">
	<%= form_for(@employee) do |f| %>

		<%= render 'fields', :f => f %>

		<div class="actions">
			<%= f.submit "Update" %>
		</div>

	<% end %>
</div>